#==============================================================================
# WOKMOK KEYS
# LAUNCH PROGRAMS
super + m
    dmenu_recency
	#rofi -show run

super + {_, shift +} semicolon
    alacritty --class float,float {-e python3,_}

super + {_, shift +} o
    alacritty --class float,float -e {htop,ranger}

# MANAGE BSPWM
# make sxhkd reload its configuration files:
super + shift + m
    pkill -USR1 -x sxhkd

# quit/restart bspwm
super + ctrl + m
	bspc wm -r

# close and kill
super + {_,shift + }c
	bspc node -{c,k}

# MOVEMENT
# focus the node in the given direction
super + {_,shift + }{h,n,e,i}
	bspc node -{f,s} {west,south,north,east}

# focus or send to the given desktop
super + {_,ctrl + }{a,s,r,t,u,y,f,w,d,j,v,apostrophe,minus}
	bspc {desktop -f,node -d} '{a,s,r,t,u,y,f,w,d,j,v,o,x}'
super + shift + {a,s,r,t,u,y,f,w,d,j,v,apostrophe,minus}
    DIR={a,s,r,t,u,y,f,w,d,j,v,o,x}; \
    bspc node -d "$DIR"; \
    bspc desktop -f "$DIR"

# preselect the direction
super + ctrl + {h,n,e,i}
    bspc node -p {west,south,north,east}

# toggle fullscreen
super + {_,shift + }l
    bspc node -t {\~floating,\~fullscreen}
